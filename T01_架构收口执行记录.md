# T01 架构收口执行记录

## 本次已完成

1. 小程序默认主线切换为云开发
2. 新增统一运行时配置文件
3. 默认关闭 HTTP API 模式
4. 修复 `admin-web/index.html` 编码，使其可按 UTF-8 维护
5. 管理后台中依赖 Node API 的扩展页面已加主线保护
6. 新增主线架构说明文档
7. 明确腾讯云服务器当前不是 MVP 必需依赖

---

## 本次改动文件

- `miniprogram/app.js`
- `miniprogram/utils/api.js`
- `miniprogram/utils/runtime.js`
- `架构与部署主线说明.md`

---

## 本次效果

- 小程序默认不再因为存在 `baseUrl` 而优先走 HTTP API
- 当前默认行为变为：优先走微信云函数
- 如果后续确实要切回 HTTP API，只能显式开启
- 管理后台点击专题、真题、导入任务时，不会再误进入 Node-only 链路

---

## 仍未完成的 T01 遗留项

1. 专题、试卷、导入任务这些扩展能力，当前仍主要依赖 Node 版接口
2. 后续需要把这些能力补齐到云开发版，才能算 T01 完整收口

---

## 下一步建议

优先继续做：

1. 把专题、试卷、导入任务补齐到云开发版
2. 再进入 `T02 微信用户身份与权限体系`
