<!--pages/activate/activate.wxml-->
<view class="container">
  <view wx:if="{{examId}}" class="exam-info-card">
    <view class="exam-info-title">当前待激活科目</view>
    <view class="exam-info-name">{{examName || '未选择科目'}}</view>
  </view>

  <view class="activate-card">
    <view class="card-title">激活科目</view>
    <view class="card-desc">输入激活码即可开通该科目</view>

    <view class="code-input-wrapper">
      <input
        class="code-input"
        type="text"
        placeholder="请输入 8-12 位激活码"
        value="{{activationCode}}"
        bindinput="onCodeInput"
        bindconfirm="onActivate"
        maxlength="12"
        auto-focus
        confirm-type="done"
      />
      <view class="code-clear" wx:if="{{activationCode}}" bindtap="onClearCode">
        <text>×</text>
      </view>
    </view>

    <view class="code-tips">
      <text class="tips-icon">提示</text>
      <text>激活码为 8-12 位字母或数字</text>
    </view>
  </view>

  <button
    class="activate-btn {{activating || !activationCode ? 'disabled' : ''}}"
    bindtap="onActivate"
    disabled="{{activating || !activationCode}}"
  >
    {{activating ? '激活中...' : '立即激活'}}
  </button>

  <view class="activated-list">
    <view class="list-title">已激活科目</view>

    <view wx:if="{{activatedExams.length === 0}}" class="empty-activated">
      <text class="empty-icon">暂无</text>
      <text>暂无已激活科目</text>
    </view>

    <view wx:else class="exam-items">
      <view
        wx:for="{{activatedExams}}"
        wx:key="_id"
        class="activated-item"
        bindtap="onExamTap"
        data-exam="{{item.examId}}"
      >
        <view class="item-icon">
          <text class="item-icon-text">{{item.examId.iconText || '书'}}</text>
        </view>
        <view class="item-info">
          <view class="item-name">{{item.examId.name}}</view>
          <view class="item-meta">
            <text class="tag tag-primary">{{item.examId.category}}</text>
          </view>
        </view>
        <view class="item-arrow">
          <text>></text>
        </view>
      </view>
    </view>
  </view>
</view>
