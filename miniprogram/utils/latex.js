// utils/latex.js
// Disabled KaTeX runtime to avoid build issues with miniprogram_npm.
function renderLatexToHtml(latex, displayMode = false) {
  return '';
}

module.exports = {
  renderLatexToHtml
};
